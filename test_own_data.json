{
  "data": {
    "filepath": "C://LIA2025LiU//2022-06-17_ST16-05//simulated_dataset",
    "target_column": "target",
    "test_size": 0.2,
    "val_size": 0.1,
    "random_state": 0,
    "dummy": false
  },

  "preprocessor": {
    "scaler": true,
    "dummy": true,
    "categorical_columns": null
  },

  "save": {
    "enabled": true,
    "dir": "artifacts",
    "overwrite": false
  },

  "models": [
        {
      "name": "RandomForest",
      "type": "random_forest",
      "build_mode":"grid",
      "n_estimators": 100,
      "max_depth": null,
      "random_state": 0,
      "scaler": false,
      "fit_predict_on": "val"
    },
        {
        "name": "KerasConv1D",
        "type": "keras_classifier",
        "build_mode": "grid",
        "scaler": true,
        "epochs": 30,
        "batch_size": 16,
        "learning_rate": 0.001,
        "callbacks": true,
        "fit_predict_on": "val",
        "layers": [

          {"type": "Conv1D", "filters": 64, "kernel_size": 5, "activation": "relu", "padding": "same"},
          {"type": "MaxPooling1D", "pool_size": 2},
          {"type": "Conv1D", "filters": 128, "kernel_size": 3, "activation": "relu", "padding": "same"},
          {"type": "MaxPooling1D", "pool_size": 2},
          {"type": "Flatten"},
          {"type": "Dense", "units": 128, "activation": "relu"},
          {"type": "Dropout", "rate": 0.4}
        ]
      },
      {
  "name": "KerasRNN",
  "type": "keras_classifier",
  "build_mode": "grid",
  "scaler": true,
  "epochs": 30,
  "batch_size": 16,
  "learning_rate": 0.001,
  "callbacks": true,
  "fit_predict_on": "val",
  "layers": [
    {"type": "Masking", "mask_value": -99999.0},
    {"type": "LSTM", "units": 64, "return_sequences": true, "dropout": 0.3, "recurrent_dropout": 0.3},
    {"type": "LSTM", "units": 128, "return_sequences": false, "dropout": 0.3, "recurrent_dropout": 0.3},
    {"type": "Dense", "units": 128, "activation": "relu"},
    {"type": "Dropout", "rate": 0.4}
  ]
}
,{
  "name": "KerasTransformer",
  "type": "keras_classifier",
  "build_mode": "grid",
  "scaler": true,
  "epochs": 30,
  "batch_size": 16,
  "learning_rate": 0.001,
  "callbacks": true,
  "fit_predict_on": "val",
  "layers": [
    {"type": "Masking", "mask_value": -1.0},

    {"type": "MultiHeadAttention", "num_heads": 4, "key_dim": 64},
    {"type": "Dropout", "rate": 0.2},
    {"type": "LayerNormalization", "epsilon": 1e-6},

    {"type": "Dense", "units": 128, "activation": "relu"},
    {"type": "Dropout", "rate": 0.3},
    {"type": "LayerNormalization", "epsilon": 1e-6},

    {"type": "GlobalAveragePooling1D"},
    {"type": "Dense", "units": 128, "activation": "relu"},
    {"type": "Dropout", "rate": 0.4}
  ]
},
{
  "name": "KerasGRU",
  "type": "keras_classifier",
  "build_mode": "grid",
  "scaler": true,
  "epochs": 30,
  "batch_size": 16,
  "learning_rate": 0.001,
  "callbacks": true,
  "fit_predict_on": "val",
  "layers": [
    {"type": "Masking", "mask_value": -99999.0},
    {"type": "GRU", "units": 64, "return_sequences": true, "dropout": 0.3, "recurrent_dropout": 0.3},
    {"type": "GRU", "units": 128, "return_sequences": false, "dropout": 0.3, "recurrent_dropout": 0.3},
    {"type": "Dense", "units": 128, "activation": "relu"},
    {"type": "Dropout", "rate": 0.4}
  ]
}

]
,
      "callbacks": [
    {
    "type": "ReduceLROnPlateau",
    "monitor": "val_loss",
    "factor": 0.5,
    "patience": 75,
    "min_delta": 0.001,
    "min_lr": 1e-6,
    "verbose": 1
  },
  {
    "type": "EarlyStopping",
    "monitor": "val_loss",
    "mode": "min",
    "patience": 200,
    "min_delta": 0.0001,
    "verbose": 1,
    "restore_best_weights": true
  }
]
        
      ,
      "notes": "Example configurable MLP. Final output layer is added automatically by the model based on number of classes."
    
  
}
